import {Input, InputNumber} from 'antd';
import styled from '~/helpers/styled-component';

export const InputCustom = styled(Input)``;

export const InputNBCustom = styled(InputNumber)`
  width: 100% !important;
`;
